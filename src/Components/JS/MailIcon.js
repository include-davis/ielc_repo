import React from 'react';

export default function InstaIcon({size}) {

    return (
        <img
            style={{
                width:`${size}`, 
                height:`${size}`
            }}
            src={"/Mail.svg"}
        />
    )
}